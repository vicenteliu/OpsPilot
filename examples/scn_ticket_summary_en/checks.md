# Contract Self-validation Checklist (English mirror)

> Same shape as `scn_ticket_summary_zh/checks.md`, with English-sample IDs.
> Any row that fails to line up = spec gap to fix in schemas/templates.

## A. Cross-file ID equality

| ID | Occurrence 1 | Occurrence 2 | Occurrence 3 |
|---|---|---|---|
| `sess_01K1A0AQXM7N7Q4G1YK6K8D6M0` | `session/meta.yaml#id` | `session/trace.jsonl[*].session_id` | `session/audit.log` (col 4) + `retrieval/request.json#session_id` |
| `doc_afe80531` | `kb/sop_vpn_en.md#frontmatter.id` | `kb/doc-meta.json#id` | `kb/chunks.jsonl[*].document_id` + `retrieval/response.json#results[*].document_id` + `harness/fixture.json#extensions.rag_ground_truth.must_retrieve_doc_ids` + `session/artifacts/art_35bdeeb64e8242c9.json#citations[0].document_id` |
| `chk_e3fe2afe` | `kb/chunks.jsonl[1].id` | `retrieval/response.json#results[0].chunk_id` | `harness/fixture.json#extensions.rag_ground_truth.must_retrieve_chunk_ids[0]` + `session/artifacts/art_35bdeeb64e8242c9.json#citations[0].chunk_id` + `harness/results.jsonl#evaluators[ev_rag_*].details.*chunk_ids*` |
| `chk_6bb25d8e` | `kb/chunks.jsonl[0].id` | `retrieval/response.json#results[1].chunk_id` | `harness/fixture.json#extensions.rag_ground_truth.should_retrieve_chunk_ids[0]` |
| `chk_e8c1c078` | `kb/chunks.jsonl[2].id` | `retrieval/response.json#results[2].chunk_id` | (no ground-truth ref) |
| `art_35bdeeb64e8242c9` | `session/artifacts/art_35bdeeb64e8242c9.json` (canonical filename) | `session/artifacts/art_35bdeeb64e8242c9.meta.yaml#artifact_id` | `session/trace.jsonl[seq=7].artifact_ids[0]` + `session/audit.log` + `harness/results.jsonl#output.artifact_id` |
| `act_01K1A0AQXM7N7Q4G1YK6K8D6MA` | `session/trace.jsonl[seq=3].action_id` | `session/trace.jsonl[seq=4].action_id` | `session/audit.log` |
| `act_01K1A0AQXM7N7Q4G1YK6K8D6MB` | `session/trace.jsonl[seq=6].action_id` | `session/trace.jsonl[seq=7].action_id` + `session/artifacts/art_35bdeeb64e8242c9.meta.yaml#produced_by.action_id` | `session/audit.log` |
| `fix_329faee7` | `harness/fixture.json#id` | `harness/golden.json#fixture_id` | `harness/results.jsonl#fixture_id` + `session/meta.yaml#labels.fixture_id` |
| `run_01K1A0AQXM7N7Q4G1YK6K8D6RR` | `harness/run-config.yaml#run.id` | `harness/results.jsonl#run_id` | — |
| `q_01K1A0AQXM7N7Q4G1YK6K8D6M1` | `retrieval/response.json#query_id` | (generated by `kb.search`) | — |

## B. Cross-directory schema bindings

| Instance file | Must validate against | Key checks |
|---|---|---|
| `kb/doc-meta.json` | `memory/schemas/kb-document.schema.json` | `redaction_passed=true`; `embedding_model` not `latest`/`auto`/`stable`; `language=en`; `namespace=opspilot:public-kb` |
| `kb/chunks.jsonl[*]` | `memory/schemas/kb-chunk.schema.json` | `content` & `content_artifact_id` mutually exclusive ✓; `line_end ≥ line_start` ✓ |
| `retrieval/request.json` | `memory/schemas/retrieval-query.schema.json#oneOf[0]` | `kind=request`; `scopes` ≥ 1; `mode=hybrid` requires `hybrid` block ✓ |
| `retrieval/response.json` | `memory/schemas/retrieval-query.schema.json#oneOf[1]` | `kind=response`; `score ∈ [0,1]`; `citation` present (because `return_citations=true`) ✓ |
| `session/meta.yaml` | `session/schemas/session.schema.json` | `model.provider_id` registered; `model.version` not `latest`/`auto`/`stable` ✓ |
| `session/trace.jsonl[*]` | `session/schemas/trace-event.schema.json` | type→props oneOf branches; ULID patterns valid ✓ |
| `session/artifacts/art_35bdeeb64e8242c9.json` | `harness/golden.json#schema_check` (incident_summary_v1) | required keys present; `scope ∈ enum`; `tasks ≥ 3`; `citations ≥ 1`; `severity` matches `^P[0-4]$` ✓ |
| `harness/fixture.json` | `harness/schemas/fixture.schema.json` | `redacted=true`; `scenario_id` pattern ✓ |
| `harness/results.jsonl` | `harness/schemas/eval-result.schema.json` | `judge_model_ref` version-pinned; `evaluators ≥ 1`; `scores.weighted ∈ [0,1]` ✓ |

## C. RAG triplet self-check

| Evaluator | Input | Computation | Result |
|---|---|---|---|
| `ev_rag_recall_at_k` (k=3) | ground_truth = `["doc_afe80531"]`<br>retrieved top-3 doc_ids = `["doc_afe80531"]×3` | matched / expected = 1/1 | **1.0 ✓** |
| `ev_rag_precision_at_k` (k=3) | relevant = `["chk_e3fe2afe", "chk_6bb25d8e"]`<br>retrieved top-3 = `[chk_e3fe2afe, chk_6bb25d8e, chk_e8c1c078]` | 2/3 relevant | **0.667 ✓** (no minimum threshold) |
| `ev_rag_citation_validity` | summary citations[0] → `chk_e3fe2afe`, lines 37-46 ↔ `kb/chunks.jsonl#chk_e3fe2afe.line_start=37 / line_end=46` | 1/1 valid | **1.0 ✓** |

## D. End-to-end flow

```
fixture.json#input
    │ (matches session/inputs/ticket.json field-for-field)
    ▼
session/meta.yaml#playbook + model_ref
    │ (model_ref → providers/templates/anthropic.config.template.yaml)
    ▼
trace.jsonl[seq=3] tool_call kb.search args
    │ (matches retrieval/request.json field-for-field)
    ▼
retrieval/response.json#results[0..2]
    │ (chunk_id ∈ kb/chunks.jsonl[*].id)
    │ (citation.line_start/end == kb/chunks.jsonl[*].line_start/end)
    ▼
trace.jsonl[seq=5] response (NL summary with [^kb-1] footnote)
    │ + trace.jsonl[seq=6,7] artifact.write
    ▼
session/artifacts/art_35bdeeb64e8242c9.json
    │ (citations[0].chunk_id == retrieval response hit)
    │ (citations[0].line_start/end == kb/chunks.jsonl#chk_e3fe2afe.line_start/end)
    ▼
harness/run-config.yaml + fixture/golden/rubric
    │
    ▼
harness/results.jsonl (8 evaluators, weighted=0.964, pass=true)
    │
    ▼
case-studies/e2e-samples/ (report archive target)
```

## E. Sandbox leftovers to tidy on host

The FUSE mount blocks `unlink` of certain dev-time files. After committing,
on your host run:

```bash
cd ~/Workspace/OpsPilot/examples/scn_ticket_summary_en/session/artifacts && \
rm -f _summary_pending.json
```

Only the canonical pair should remain:
- `art_35bdeeb64e8242c9.json`
- `art_35bdeeb64e8242c9.meta.yaml`

## F. Machine-runnable checks (pseudocode)

> Same shape as `scn_ticket_summary_zh/checks.md#F`. Implementation lives
> outside the spec stage.

```python
# 1. ID set equality
assert ids_in("session/trace.jsonl") ⊇ ids_in("session/audit.log")
assert chunk_ids_in("retrieval/response.json") ⊆ chunk_ids_in("kb/chunks.jsonl")
assert artifact_id_in("trace.jsonl") == filename_stem_of("session/artifacts/art_*.json") == sidecar.artifact_id

# 2. Every schema reference resolves
for f, schema in mapping.items():
    jsonschema.validate(load(f), load(schema))

# 3. Citation line ranges match
for cit in artifact.citations:
    chunk = chunks_by_id[cit.chunk_id]
    assert chunk.line_start == cit.line_start
    assert chunk.line_end   == cit.line_end
    assert chunk.document.source_path == cit.source_path

# 4. RAG evaluators recomputable end-to-end
recall = recompute_recall_at_k(fixture.ground_truth, response.top_k_docs)
assert isclose(recall, results.evaluators["ev_rag_recall_at_k"].score)

# 5. All model refs version-pinned
assert not any(ref.endswith(("@latest", "@auto", "@stable"))
               for ref in collect_model_refs())
```
