id: "act_01SANDBOX0EXAMPLE000000000000"
session_id: "sess_01SANDBOX0EXAMPLE000000000000"
proposed_by: "model:anthropic:claude-haiku-4-5:2026-04"
created_at: "2026-05-05T00:00:00Z"
type: "shell"
description: "Check disk usage and uptime inside a hardened L2 sandbox container."
target_environment: "dev"

payload:
  command: "df -h /work && uptime && echo 'sandbox OK'"
  shell: "/bin/sh"
  workdir: "/work"
  env: {}

requested_policy:
  network:
    mode: "deny-all"
  fs:
    rootfs: "read_only"
    workdir: "/work"
  resource:
    cpu: "0.5"
    memory: "256Mi"
    disk_tmpfs: "32Mi"
    pids: 64
    timeout_seconds: 15
  secrets: []

rollback_hint:
  irreversible: false
  steps: []

dry_run: true
approval_required: false
