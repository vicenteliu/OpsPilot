{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opspilot.dev/schemas/kb-document.schema.json",
  "title": "KB Document",
  "description": "Long-term knowledge-base document metadata (markdown source + derived indices)",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "source_path",
    "title",
    "classification",
    "content_hash",
    "ingested_at",
    "language",
    "namespace",
    "chunk_strategy",
    "embedding_model",
    "embedding_dim",
    "redaction_passed"
  ],
  "properties": {
    "_comment": {
      "type": "string",
      "description": "Optional human-readable comment; ignored by tooling."
    },
    "id": {
      "type": "string",
      "pattern": "^doc_[a-f0-9]{8}$",
      "description": "doc_<sha8>, derived from source_path + the initial content_hash"
    },
    "source_path": {
      "type": "string",
      "description": "Repo-relative path (markdown)"
    },
    "source_url": {
      "type": ["string", "null"],
      "format": "uri",
      "description": "Original URL when imported from an external wiki / Confluence / Notion"
    },
    "title": { "type": "string", "minLength": 1 },
    "classification": {
      "type": "string",
      "enum": ["public", "internal", "confidential", "restricted"]
    },
    "content_hash": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$"
    },
    "version": {
      "type": ["string", "null"],
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "ingested_at": { "type": "string", "format": "date-time" },
    "last_modified": { "type": "string", "format": "date-time" },
    "language": {
      "type": "string",
      "examples": ["zh-CN", "en", "mixed"]
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" }
    },
    "namespace": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*:[a-z][a-z0-9_-]*$"
    },
    "chunk_strategy": {
      "type": "string",
      "enum": [
        "headings_then_size",
        "fixed_size",
        "sentence_boundary",
        "code_aware",
        "semantic"
      ]
    },
    "chunk_count": { "type": "integer", "minimum": 0 },
    "embedding_model": {
      "type": "string",
      "description": "<provider_id>/<name>@<version>; latest / auto / stable are forbidden",
      "not": { "pattern": "@(latest|auto|stable)$" }
    },
    "embedding_dim": { "type": "integer", "minimum": 32 },
    "redaction_passed": {
      "type": "boolean",
      "const": true,
      "description": "Documents that fail the redaction check must not enter the index"
    },
    "redaction_rules_version": { "type": "string" },
    "license": {
      "type": ["string", "null"],
      "description": "Required when sourced from a third-party public dataset"
    },
    "extensions": {
      "type": "object",
      "default": {}
    }
  }
}
