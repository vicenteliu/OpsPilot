# direnv config; auto-loaded in this folder.
# `direnv allow` once after editing.

dotenv_if_exists .env

# Activate venv if present
if [ -d ".venv" ]; then
  source_env_if_exists .venv/bin/activate
fi
